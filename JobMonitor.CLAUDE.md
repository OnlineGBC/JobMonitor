# Instructions

- Only make changes that are explicitly requested
- If you notice something else that might need attention, ask first and wait for approval before making any changes
- Do not overstep the scope of what was asked
- Automatically commit with a meaningful message and push after every code change
- Do not include "Co-Authored-By" lines in commit messages
- Keep this file in sync with ~/.claude/CLAUDE.md (when updating either file, update both)
- When debugging issues, check for environmental causes first (concurrent processes, file locking, resource contention, disk space, memory, network) before blaming external factors
